name: kunsendi
description: A practice project for an image sharing app :)

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  google_fonts: ^1.1.1

  http: ^0.12.2
  flutter_secure_storage: ^3.3.5
  shared_preferences: ^0.5.12+4
  jwt_decoder: ^1.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - google_fonts/
